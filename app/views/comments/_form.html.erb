
<%=form_for ([@noticium, @noticium.comments.build]) do |f|%>
<% if @comment_build[0].errors.any? %>

<% end %>
  <div class="field">
    <%= f.label :nombre %>
    <%= f.text_field :nombre %>
  </div>

  <div class="field">
    <%= f.label :content %>
    <%= f.text_area :content %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<%end%>
